// Función para verificar si un valor es distinto de otro
export function esDiferente(valor1, valor2) {
  return valor1 !== valor2
}

// Función para verificar si un array no contiene un elemento
export function noContiene(array, elemento) {
  return !array.includes(elemento)
} 